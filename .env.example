GEMINI_API_KEY=

# Optional, web/app.py only: speeds up OCR on scanned PDFs by using Google
# Cloud Vision instead of local easyocr. Without it, OCR still works, just
# much slower. See web/README.md "Scanned PDFs and OCR speed".
GOOGLE_CLOUD_VISION_API_KEY=
